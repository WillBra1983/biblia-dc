# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# O build Android compete com o SO, Chrome, etc. Em PCs com ~4GB ou menos,
# -Xmx alto + vários workers costuma derrubar o daemon (hs_err_pid*.log:
# "Native memory allocation (mmap) failed"). Ajuste conforme a sua RAM:
#   • 8GB+  : -Xmx2048m -XX:MaxMetaspaceSize=512m e pode comentar workers.max
#   • 4–8GB : valores atuais (signReleaseBundle precisa de metaspace ≥ 384m)
#   • ≤4GB  : comente a linha abaixo e use: .\scripts\bundle-release-lowmem.ps1
org.gradle.jvmargs=-Xmx1280m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=128m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Um worker reduz picos de RAM durante compile/dex (recomendado em 4GB ou menos).
org.gradle.workers.max=1
org.gradle.parallel=false
org.gradle.configureondemand=false

# Daemon Kotlin separado — evita segundo processo JVM com heap grande.
kotlin.daemon.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
android.enableJetifier=true
android.suppressUnsupportedCompileSdk=35

kotlin.incremental=true
kotlin.code.style=official

# Fix for BouncyCastle issues
org.gradle.daemon=true
