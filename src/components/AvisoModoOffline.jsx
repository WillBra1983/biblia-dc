/**
 * Faixa global de offline removida — o usuário não precisa saber que está sem internet
 * durante a leitura local. Use `MSG_SEM_INTERNET_RECURSO` (snackbar) ao abrir recurso da nuvem/IA.
 */
export default function AvisoModoOffline() {
  return null
}
